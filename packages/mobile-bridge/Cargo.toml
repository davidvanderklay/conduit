[package]
name = "conduit-mobile"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "Stable mobile FFI boundary for the Conduit media engine"

[lib]
crate-type = ["cdylib", "staticlib", "rlib"]

[dependencies]
conduit-core = { path = "../core" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[target.'cfg(target_os = "android")'.dependencies]
jni = "0.22"
